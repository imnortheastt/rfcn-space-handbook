---
/**
 * References — auto-collected bibliography section.
 * Blueprint §4.2 + FR-06: citation rendering at lesson end.
 * Full cite component is wired in Phase 4 (packages/citations).
 */
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<section class:list={['lesson-references', className]} aria-label="References">
  <slot />
</section>
